使用一个大小为128的整数数组来记录每个ASCII字符最后一次出现的位置，并全部初始化为-1表示尚未出现。程序通过滑动窗口思想，用left和right两个指针遍历字符串：右指针right逐字符前进，若当前字符在数组中记录的位置大于等于左边界left，说明字符在窗口内重复，就将左边界left移动到该字符上次位置的下一位。每处理一个字符后，立即在数组中更新它的最新位置为当前索引right，并计算当前窗口长度（right-left+1），同时不断更新记录的最大长度。